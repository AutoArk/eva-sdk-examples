[project]
name = "eva-python-voice-dialogue-agent"
version = "0.1.0"
description = "Terminal voice dialogue demo for the EVA Python Client SDK"
requires-python = ">=3.11"
dependencies = [
    "autoark-eva-client-sdk[pyaudio,camera]==0.0.1.dev1",
    "numpy>=1.26",
]

[dependency-groups]
dev = ["pytest>=8.4"]

[tool.uv.sources]
autoark-eva-client-sdk = { index = "eva-testpypi" }

[[tool.uv.index]]
name = "eva-testpypi"
url = "https://test.pypi.org/simple"
explicit = true

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["error"]
